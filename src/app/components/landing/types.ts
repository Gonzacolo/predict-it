export type LandingStat = {
  value: string;
  label: string;
};

export type LandingStep = {
  num: string;
  title: string;
  description: string;
};

export type LandingFaq = {
  q: string;
  a: string;
};
